name: Domain

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    paths: 
      - "src/HealthTracking.Domain/**" 
      - "tests/HealthTracking.Domain.Tests/**" 
      - ".github/workflows/domain.yml"

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v5
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore domain
        run: dotnet restore src/HealthTracking.Domain/HealthTracking.Domain.csproj

      - name: Build domain
        run: dotnet build src/HealthTracking.Domain/HealthTracking.Domain.csproj --configuration Release --no-restore

      - name: Restore tests
        run: dotnet restore tests/HealthTracking.Domain.Tests/HealthTracking.Domain.Tests.csproj

      - name: Run tests
        run: dotnet test tests/HealthTracking.Domain.Tests/HealthTracking.Domain.Tests.csproj --configuration Release --no-restore
